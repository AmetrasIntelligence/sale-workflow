#. Go to **Sale > Settings > Default Sort Criteria** and select one.
#. You must select sort direction too.
#. Create a new sale and add several lines.
#. On saving sale order you must see how the sale lines are ordered
   with your selected sort order.
